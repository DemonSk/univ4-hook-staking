export const metadata = {
  title: "Uniswap v4 Hook Staking",
  description: "Stake token to earn fees from hook",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
